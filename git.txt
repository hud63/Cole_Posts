# 1) Move into your existing project folder
cd "Vanderbilt Money"

# 2) Add a minimal homepage
cat > index.html <<'HTML'
<!doctype html>
<html>
  <head><meta charset="utf-8"><title>Hello Pages</title></head>
  <body><h1>It works!</h1><p>Deployed with GitHub Pages.</p></body>
</html>
HTML

# 3) Initialize git on main and commit
git init -b main
git add .
git commit -m "Initial commit"

# 4) Create the remote repo on GitHub and push to it
REPO_NAME="Cole_Posts"
gh repo create "$REPO_NAME" --public --source=. --remote=origin --push
